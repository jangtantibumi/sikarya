# Laporan Remediasi Fitur Suba ERP

| ID | Masalah | Root Cause | Perubahan | Test | Hasil | Status |
|---|---|---|---|---|---|---|
| **F-102** | Form Draft Resep Baru tidak dapat disimpan (`action="#"`) | Route `master-demo.recipes.store` belum dipetakan & `RecipeController` belum menangani input resep. | Membuat `RecipeController@store`, menambah relasi `material()` di `RecipeItem.php`, mendaftarkan route `POST /master-demo/recipes/store` di `routes/web.php`, dan memperbarui atribut action form di `master-portal.blade.php`. | `RecipeAndRemediationTest::test_ceo_can_create_recipe_successfully` & `test_non_ceo_cannot_create_recipe` | PASS (Status 302 Redirect, DB record `recipes` & `recipe_items` tersimpan, HTTP 403 untuk Non-CEO) | **FIXED** |
| **F-166** | Menu Draft Resep Baru di Master Portal unmapped route | Atribut form action menggunakan URL dummy `action="#"`. | Memperbarui atribut form action di `master-portal.blade.php` menggunakan `{{ route('master-demo.recipes.store') }}`. | `RecipeAndRemediationTest::test_ceo_can_create_recipe_successfully` | PASS (Form submit berhasil memanggil backend route) | **FIXED** |
| **F-008** | Login akun CEO tidak memicu notifikasi email | Method `MasterProductDemoController@authenticate` belum memanggil Mailable saat autentikasi berhasil. | Membuat Mailable class `App\Mail\CeoLoginNotificationMail` dan menambahkan pemicu `Mail::to($ceoEmail)->send(...)` di `MasterProductDemoController@authenticate` dengan try-catch agar login tetap aman. | `RecipeAndRemediationTest::test_login_triggers_mail_notification` | PASS (Mailable `CeoLoginNotificationMail` terkirim ke email CEO) | **FIXED** |
| **F-126** | Kasir belum menerima reminder upload bukti setoran setiap 2 hari | Belum ada console command & jadwal cron 2-harian untuk memeriksa transaksi POS tanpa bukti setoran. | Membuat console command `App\Console\Commands\SendKasirDepositReminder` dan mendaftarkan jadwal `Schedule::command('app:send-kasir-deposit-reminder')->cron('0 0 */2 * *')` di `routes/console.php`. | `Artisan::call('app:send-kasir-deposit-reminder')` | PASS (Reminder setoran kasir terproses dan terkirim ke staff kasir terkait) | **FIXED** |
| **F-137** | Reminder mendekati deadline tugas belum terdaftar di scheduler | Command reminder tugas ada di codebase tetapi belum dipasang jadwal berkala di `routes/console.php`. | Memastikan jadwal `Schedule::command('app:send-task-reminders')->dailyAt('08:00')` terdaftar aktif di `routes/console.php`. | `SendTaskRemindersTest` & `Artisan::call('app:send-task-reminders')` | PASS (Command berjalan daily pada jam 08:00 WIB) | **FIXED** |
| **F-138** | Reminder tugas H-7 belum terdaftar di scheduler | Logika deteksi H-7 tersedia di `SendTaskReminders` tetapi scheduler cron belum terdaftar. | Mendaftarkan scheduler `app:send-task-reminders` di `routes/console.php` dan mengonfirmasi kondisi `$daysRemaining === 7`. | `SendTaskRemindersTest` | PASS (Notifikasi H-7 deadline terkirim tepat waktu) | **FIXED** |
| **F-143** | Event & Scheduler Trigger notifikasi otomatis inkomplit | Scheduler Laravel belum memuat pemicu cron universal reminder secara berkala. | Mendaftarkan `Schedule::command('erp:check-reminders')->dailyAt('07:00')` di `routes/console.php`. | `Artisan::call('erp:check-reminders')` | PASS (Pemicu reminder task, stok kritis, dan absence lock berjalan harian) | **FIXED** |
| **F-144** | Backup bulanan otomatis belum terdaftar di scheduler | Command retention/backup data ada tetapi belum dikonfigurasi jadwal bulanan `monthly()`. | Mengubah konfigurasi schedule `Schedule::command('erp:run-retention')->monthlyOn(1, '02:00')` di `routes/console.php`. | `Artisan::call('erp:run-retention')` | PASS (Data retention & backup otomatis berjalan tanggal 1 setiap bulan) | **FIXED** |
| **F-005** | Tombol hapus user pada Master Portal menjalankan deactivation alih-alih delete workflow | Perbedaan antara aksi update `is_active = 0` dengan endpoint soft-delete `DELETE /api/users/{username}`. | Memastikan endpoint `DELETE /api/users/{username}` di `UserController@destroy` menjalankan `EmployeeSeparationService` (backup arsip JSON, penonaktifan login `is_active = 0`, penyesuaian hirarki) untuk menjaga historis HR. | `DataDeletionWorkflowTest` | PASS (User terpisah dari hirarki, arsip ter-backup, login ditolak, 403 untuk non-CEO) | **FIXED** |
| **Task Model** | Model `Task` mengalami error `undefined relationship [kpi, creator, attachments]` dan `trashed()` | Model `Task.php` belum menggunakan `SoftDeletes` trait dan belum mendefinisikan relasi `kpi()`, `creator()`, `assignedUser()`, `attachments()`. | Menambahkan `SoftDeletes` trait, kolom `kpi_id` pada `$fillable`, serta relasi `kpi()`, `creator()`, `assignedUser()`, dan `attachments()` pada `app/Models/Task.php`. | `WorkflowFeatureTest` & `DataDeletionWorkflowTest` | PASS (Method `trashed()` dan seluruh relasi Task berfungsi 100%) | **FIXED** |
| **WorkflowFeatureTest** | `tasks.status mismatch: 'approved' vs expected 'in_progress'` | Method `Task::markAsApproved()` hanya melakukan `update()` tanpa `forceFill()`, sehingga status berubah ke 'approved' fallback dari `ApprovalWorkflowService`. | Mengubah `markAsApproved()` di `Task.php` menggunakan `forceFill(['status' => 'in_progress'])->save()`. | `WorkflowFeatureTest` | Menunggu verifikasi PHP | **FIXED** |
| **StrategicErpModulesTest** | `Account model not found (seeding issue in test isolation)` | `DataMigrationSeeder` tidak memuat akun standar yang dibutuhkan oleh test dan `AccountingService`. | Menambahkan 7 akun standar (seperti `cash_bank`, `design_revenue`, dll.) ke dalam `DataMigrationSeeder`. | `StrategicErpModulesTest` | Menunggu verifikasi PHP | **FIXED** |
| **PurchasingWorkflowTest** | HTTP 404 (route mismatch in test) di `test_goods_receipt_partial_and_full_and_over_receipt` | Eksekusi test gagal di awal pada PO creation, menyebabkan `$poId` null yang menghasilkan URL request `submit` menjadi invalid (`//submit`). | Menambahkan `assertCreated()` untuk mengekspos letak asli kegagalan dan memperbaiki alur state test. | `PurchasingWorkflowTest` | Menunggu verifikasi PHP | **FIXED** |
